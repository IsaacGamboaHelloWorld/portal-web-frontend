import { CardFranchiseTypePipe } from './card-franchise-type.pipe';

describe('CardFranchiseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CardFranchiseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
