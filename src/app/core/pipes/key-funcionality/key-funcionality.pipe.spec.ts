import { KeyFuncionalityPipe } from './key-funcionality.pipe';

describe('KeyFuncionalityPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyFuncionalityPipe();
    expect(pipe).toBeTruthy();
  });
});
