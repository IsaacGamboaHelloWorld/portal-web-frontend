import { KeyFuncionalityAssetsPipe } from './key-funcionality-assets.pipe';

describe('KeyFuncionalityAssetsPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyFuncionalityAssetsPipe();
    expect(pipe).toBeTruthy();
  });
});
