import { CardTypeclassPipe } from './card-type-class.pipe';

describe('CardTypeclassPipe', () => {
  it('create an instance', () => {
    const pipe = new CardTypeclassPipe();
    expect(pipe).toBeTruthy();
  });
});
